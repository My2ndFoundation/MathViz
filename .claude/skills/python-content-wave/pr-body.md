# PR 描述模板

标题：`python {{期}} · {{波名}}：{{页列表}}`。每个 `{{…}}` 按实际填；**验证一节只写真做过的事**，没做的列为未勾选项。
末尾保留生成署名行。

---

{{一句话：这一波做了哪些页、属于哪个模块、清单出处}}

## 新增
| 页 | 程序数 | 行数 | 变体组 | 带 property 检查 |
|---|---|---|---|---|
| {{py-…}} | {{n}} | {{lines}} | {{组名列表}} | {{k}} |

门数 {{前}} → {{后}}（若有变化，写明新增了哪道门、为什么）。

## 评审
整波终审一次（{{模型}}），发现 {{Critical/Important/Minor 数}}；修复一轮、范围复审 {{结论}}。
留到账本、未在本 PR 处理的：{{列表或「无」}}。

## 验证
- [x] 全量验收命令本机全绿：`check.py` {{N}} 道门、导航契约 {{M}} 项、四道根级门、chess / cryptography 门、三个生成脚本 `--check`、core 测试。
- [x] 每页至少一个 P 程序由控制方亲手做负控制：因断言失败而红，从内存原字节复原。
- [x] 浏览器（浏览器工具不能操作 `file://`，所以改用 http 预览打开本分支的 worktree，探针先确认读到的是本分支）：中英 × 读 / 挖空 / 临摹；面板元数据；每个程序都有空；字面量反馈不印原文；临摹三层在 0.9 / 1 / 1.25 缩放下对齐（量坐标）。
- [x] 复制内容真跑：每页按种子 {{seed}} 抽 3 个程序（{{id 列表}}），三种模式的复制内容相同，python3 跑出的 stdout 与 `run.expect` 逐字节相同。
- [ ] **`file://` 双击打开新页面、复制程序粘进 PyCharm 真跑——需要人来做**
- [ ] CI

🤖 Generated with [Claude Code](https://claude.com/claude-code)
