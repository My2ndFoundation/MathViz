# 阶段 9f：`fieldBFS`（多源 BFS，最少步数场）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给工具⑤ 加**第九道题** `fieldBFS`：在 8×8 盘上从**多个起点同时**做 BFS，铺出整张「最少步数场」，每格的标量**立成一根柱子**。

**这道题存在的全部理由**：它与 `knightPath` **只差一行**（队列初始塞 N 个起点而不是 1 个）。**教的是迁移 —— 迁移的代价是一行。** `tips` 必须明说，否则它只是又一道 BFS。

**Tech Stack:** 零依赖 ES 子集 JavaScript；`chess/scripts/check.py` 与 `inline_core.py`；无构建。

---

## Global Constraints

**每次派发子代理都必须带上这一句：**

> 如果计划里某段代码是错的、或某个测试断言了一件事实上错误的事，停下来报告，不许改代码或改测试去迁就它。计划是记录，不是权威。

⚠⚠ **这份计划的作者到 9e 为止已经写错过十二处**，**十二次全部是实现者或审查者干活时撞见的，没有一次是机器门拦下来的**。最近这几处尤其说明问题：

| # | 阶段 | 写的 | 实际 |
|---|---|---|---|
| 5 | 9d | 深度塔「恰好八层」 | **九层** —— 差点把一份正确实现判为不通过 |
| 7 | 9d | tips 删 `& NOT_A` / `& NOT_AB` | **删了什么都不会发生**，那一课当场蒸发 |
| 9 | 9d | 订正 #5 时写「后三张掩码最高位都是 0」 | **订正本身引入新假话** |
| 10 | 9e | 「`N ≥ 3` 是那条优化的正确性前提」 | **不成立**，凭推理立了一条不存在的前提 |
| 11 | 9e | `191,021 + 2,175 = 193,196` | **193,200** —— 算式自洽但**少数了一项** |
| 12 | 9e | 「塔 = SQ + 1 层」 | 那是**调用栈深度**；画出来的塔是 SQ。**订正时又只扫了三处、漏了五处** |

**病因同一个：写下一个看着合理的数或推论，而没有去数它 / 跑它。**
**第 9、10、12 处更要记住：连订正都可能是错的；听起来成立的推理也可能是错的；清掉一处不等于清干净。**

规矩：

- **凡是本计划引用既有函数、字段、常量或项目规矩的地方，动手前自己 `grep` 一次再用。**
- ⚠ **光 grep 到名字存在还不够** —— 要把示例代码**放回它要插入的那段上下文里读一遍作用域**；有些只能**自己跑一遍数出来**。
- **下面标「⚠ 量出来的」的是我这次真跑过的**；标「⚠ 推的」「⚠ 选出来的」的**没量过，你自己验**。

### 工作目录

**`/private/tmp/claude-501/-Users-nickma-Develop-My2ndBrain-MathViz/76a3810e-c1e3-4111-aab6-1a281211fb48/scratchpad/wt-9f`**（worktree，分支 `claude/chess-phase9f`，从 `origin/main @ 9b4aa27` 切出）。

**每个任务第一件事**：

```bash
git -C $W rev-parse --abbrev-ref HEAD   # 必须是 claude/chess-phase9f
git -C $W status --short                # 必须是空的
```

对不上就停下来报告。⚠ **主工作树 `/Users/nickma/Develop/My2ndBrain/MathViz` 是别的 session 的活，一个字都不要动**（9e 期间它被别人推进过至少两次）。⚠ **Bash 的 cwd 会悄悄回到主工作树** —— 每条命令都用 `git -C $W` 或绝对路径。

### 规格

`docs/superpowers/specs/2026-08-02-chess-viz-suite-design.md`：

- **§4⑤「9f 开工前定下的（2026-08-10）」六条 —— 这一段的裁定书，先读它**；
- **§4⑤「探针跑完的裁定（2026-08-09）」—— 柱高与 0 值的现成收据，9c 已经付清，⚠ 不许重裁**；
- 「四道题的裁定」、可扩展性架构要求与**声明层三条硬约束**、§2.4、§5.3、§7。

### 硬约束

- **格子编号 `sq = r * W + c`**，`r=0` 是**最下面**一行、`c=0` 最左。
- **`render(parts, lang)` 那一整段在所有 algos 里逐字节相同**，`bilingual_algos_check()` 逐字节核对。**原样抄，一个字节都不许改**（⚠ 现有 11 份；那段注释里写的「七份」是陈旧数字，**不要跟着改**，另有独立任务在处理）。
- **两种语言的行数必须逐段相等**（§1.6）。写不成等行数**停下来报告**。
- **`source(opts)` 的 `lang` 必填、无默认值。**
- **生成出来的教学源码，读者是一个没学过棋、正在学算法的十六岁学生。**
- **生成源码受 ES 子集约束**：**没有模板字面量 / 正则字面量 / `for…in` / `try/catch` / 解构**，字符串用 `+` 拼。
- **宿主桥接只有五个**（`mark`/`place`/`clear`/`log`/`attacked`），**不许加第六个**。
- **`T.throws` 的 pattern 必须含守卫自己的固定文案**（9a 第九道门）。⚠ 9d 实测的坑：只含固定文案在某些守卫上**必然被判钝**，写成「完整消息 + 那一次的插值」。
- **只暂存显式路径**；禁止 `git add -A` / `git commit -a`。⚠ 钩子会**再暂存**，**commit 跑完再看一遍**。
- ⚠ 改了 `chess/core/**` 的任务有个两难（9d/9e 实测）：标记行加了名字而内联块还没生成时 `algos_roundtrip_check` **必然**报错。**出路**：跑 `inline_core.py --print-changed`，然后在 `git add` 里**显式列上**被它改过的那几份 HTML。
- ⚠ **突变实验前 `cp` 备份到 scratchpad（带任务前缀）**，**绝不用 `git checkout --` 还原**。
- ⚠ **只看 `check.py` 的退出码会误判** —— 要找到你想验的**那道门自己那一行**。
- **scratchpad 是共享的**，临时文件一律带任务前缀（`9f-t1-` 等）。
- **浏览器**：⚠ `document.hidden` 恒 true、rAF 停摆。走通的路是**scratchpad 拷贝 + rAF 垫片 + 本地 http 服务**（仓库文件不动），**带显式 `tabId`** 并**先断言端口/路径**。⚠ **`computer` 的显式 `coordinate` 在这个环境里点不中（三种映射都试过），用 `ref`**。⚠ `python3 -m http.server` **别加 `--bind 127.0.0.1`**（只监听 IPv4，浏览器走 `::1` 会被拒）。⚠ `claude-in-chrome` 连不上本地端口，用 `Claude_Browser` 的 `preview_start`。
- ⚠ **本地绿 ≠ CI 绿**。开 PR 后 `gh pr checks <PR#>`。

---

## 这道题的数（**全部实测**）

拿真的 `interp.js` 跑 ES 子集源码（含 `mark`），盘 8×8、源在四角（a1 → h8 → a8 → h1）：

| 源数 | 最远距离 | 步数 | 占 `STEP_LIMIT`(200000) | 不可达格 |
|---|---|---|---|---|
| 1 | **6** | 5,157 | 2.6% | **0** |
| 2 | **5** | 5,149 | 2.6% | **0** |
| 4 | **3** | 5,133 | 2.6% | **0** |

**余量充足，这一段没有 9e 那种可行性风险。**

**⚠ 那个「最远距离 6 → 5 → 3」就是这道题的全部教学信号**：多几个起点，整块盘都更近。

`k = 2.8 / 6 ≈ 0.467`（⚠ **推的，见规格 §4⑤ 9f ③**，与工具① 的 `K_FIELD = 0.35` 有意不同，**要在浏览器里对照一眼**）。

---

## 文件结构

| 文件 | 责任 | 本阶段动作 |
|---|---|---|
| `chess/core/algos/field-bfs.js` | 第九道题的**算法源码生成器** | **新建**（双语） |
| `chess/core/algos/field-bfs.test.js` | 它的测试 | **新建** |
| `chess/tools/chess-board-algorithms.html` | 工具⑤ | **立柱渲染路径**（付账）+ 声明 `PROBLEMS.fieldBFS` + 标记行 + 文案 + 定版 1.7.0 |
| `chess/chess-tools.json` | 注册表 | 1.6.0 → 1.7.0 |

⚠ **本阶段额外要动的只有一处：立柱渲染路径**（规格 §4⑤ 9f ④）。**报告里要把它与「这道题的开销」分开讲** —— 它是「每格标量怎么呈现」这件事的债，跟 9c 的 `kind` 放宽、9d 的 `fmtBB`、9e 的 `spanZ` 同一类。

---

## 并行编排

| 轮 | 任务 | 依据 |
|---|---|---|
| 1 | **Task 1 单独** | 生成器是后面所有任务的输入 |
| 2 | **Task 2 → 3 串行** | 两个都改 `chess-board-algorithms.html` |
| 3 | **Task 4 单独** | 浏览器验收 |
| 4 | **Task 5 单独** | 定版收尾 |

---

## Task 1：`field-bfs.js` 双语生成器 + 它的测试

**Files:** Create `chess/core/algos/field-bfs.js`、`chess/core/algos/field-bfs.test.js`

**Produces:** `require('./field-bfs.js').source({ sources, lang })` → 源码字符串。入口 **`expand`**，顶层 `… return expand(SRC, 0);`。

- [ ] **Step 1: 抄那段逐字节相同的双语渲染助手**（从 `independent.js` 或任一份切出来，⚠ 逐字节，含那句陈旧的「七份」）。抄完自己 `diff` 一次。

- [ ] **Step 2: ⚠ 先把 `knight-path.js` 整个读一遍**

**这道题是它的「差一行」版本**，所以：**能照抄的都照抄**（`DX`/`DY`、`onBoard`、`expand` 的逐层同步形态、双语散文的语气）。**报告里要写明你照抄了哪些、改了哪些** —— 「差别只有一行」这句话**必须在代码上是真的**，否则 `tips` 就是在撒谎。

⚠ **`knight-path.js` 里有 BLANK 挖空指令行**（`// >>> BLANK id=… `）。**这道题一个挖空都不做**（规格：挖空是 opt-in，不在本阶段），所以那些行**不要抄**。

- [ ] **Step 3: 写会失败的测试**

判据照 §7.3：**跟另一份思路完全不同的实现一致**。宿主侧参照**自己写一份**（比如一次性 `while` 队列的教科书 BFS，与被测的「逐层同步 + 递归」形态不同），并在注释里写明为什么这样才叫独立。

必须有的断言：

1. **三档最远距离**（1 / 2 / 4 个源）与宿主侧参照一致 —— ⚠ **别抄我表里的 6 / 5 / 3，自己算出来**。
2. **每一格的距离**都与参照逐格一致（不是只比最远值）。
3. **⚠ 源格自己的距离是 0，而且 0 真的通过 `mark` 通道送出去了** —— 这是「探针跑完的裁定②」的那一半（**0 是真值，不是「没有值」**）。用打桩 host 断言收到的是**数值 0**，不是字符串、不是缺席。
4. **`mark` 的值域**：8×8 上收到的所有值都在 0..6，且**每一格恰好被 mark 一次**（BFS 不该重复标）。
5. **⚠ 与 `knightPath` 的「差一行」**：把 `SRC` 换成单个起点时，**每格距离必须与 `knight-path.js` 跑出来的 `dist` 一致**。⚠ 这一条要**真的去调 `knight-path.js`**，不是自己再写一遍 —— 它是 `tips` 那句话唯一的机器证据。
6. **三道双语门**（行数 / 步数 / 英文变体无汉字）。⚠ **先判断第三道门在这份文件上是不是恒真**（9d 是恒真、9e 有牙，取决于有没有注释外的字符串字面量）—— **是恒真就在注释里写明它在这里没有牙**，不许让一道门看起来在守什么。
7. **`sources` 的守卫**：缺席、空数组、越界、重复、非数组各一条 `T.throws`。⚠ pattern 见 Global Constraints 那条「钝」的坑。

- [ ] **Step 4: 跑一遍确认它失败** → `Cannot find module './field-bfs.js'`

- [ ] **Step 5: 写生成器**

⚠ **可执行部分照 `knight-path.js` 抄**，**唯一实质差别是种子那一步**：

```js
// knightPath:  steps = expand([START], 0);
// fieldBFS:    每个源 dist = 0、mark 一次，然后 expand(SRC, 0)
```

⚠ **四处必须自己验**：

1. **`expand` 的返回值是「整张场的最大值」**（规格 §4⑤ 9f ⑤），不是路径长度。自己确认它在 1/2/4 源下分别给出你 Step 3 算出来的三个数。
2. **每格恰好 mark 一次** —— 逐层同步 BFS 里 `dist[nb] < 0` 那道闸是唯一的保证，自己验。
3. **源格的 0 是在哪儿 mark 的** —— 在种子那一步，不在 `expand` 里。确认它真的发出去了（Step 3 第 3 条盯着这个）。
4. **ES 子集** —— 最硬的验法是**把生成源码真喂给 `interp.js` 跑**，中英各跑。

- [ ] **Step 6: 写双语散文**，两边行数相等。要讲到的三件事：
   1. **这是 `knightPath` 的同一段代码** —— 逐层同步 BFS，一层就是一圈波纹；
   2. **⚠ 唯一的差别是队列初始塞了几个起点** —— **这句话要写得让她能自己去 `knightPath` 那个 tab 对着看**；
   3. **多个起点同时铺开，等于「离最近的那个起点几步」** —— 这就是「场」的含义。

- [ ] **Step 7: 跑一遍确认通过 + 量三档步数与最远距离**（基准 5,157 / 5,149 / 5,133 步；6 / 5 / 3）

- [ ] **Step 8: 突变验证 —— 「差一行」是真的吗**
   把种子那一步改成只塞第一个源，**期望：结果退化成 `knightPath` 的单源场**（最远距离回到 6），且 Step 3 第 5 条那条断言**仍然绿**。**这一条证明那句话不是文案吹的。** 验完 `cp` 还原。

- [ ] **Step 9: Commit**（显式路径）

---

## Task 2：立柱渲染路径（**付账**）+ 声明 `PROBLEMS.fieldBFS`

**Files:** Modify `chess/tools/chess-board-algorithms.html`

⚠ **这一任务有两件事，报告里必须分开讲**：**(A) 立柱渲染**是这一阶段唯一「额外要动的」一处（付「呈现」那笔账）；**(B) 声明**才是「这道题的开销」。

- [ ] **Step 1: 标记行加 `,field-bfs.js`**（⚠ 内联镜像那个两难见 Global Constraints）

- [ ] **Step 2: (A) 立柱渲染 —— 声明驱动，⚠ 不许按题目名分支**

⚠ **先把工具① 的 `drawHeightField`（`chess/tools/chess-moves-geometry.html:2594-2623`）整段读一遍**，再决定怎么搬。

**照抄时三处要改**（规格 §4⑤ 9f ④）：
1. `k * d` → **`base[2] + k * d`**（工具⑤ 的 `BR.layout` 接 `spec.z`）；
2. 2.8 是照边长 8 定的（本题盘固定 8×8，正好不必操心）；
3. ⚠ **工具① 把 `8` 写死在网格循环里两处**（`:2613` / `:2618`）—— **必须改成盘面尺寸**，否则将来别的盘宽会悄悄画错半张网格。

**落点**：`drawMarks(C, P, L, c, tally, pid)`（`:10451`）是每帧画标记那一段，9c 的「写数字」正是在它里面按 `info.text` 分出来的。**立柱走同样的声明驱动路子**（例如 `P.heightField`）。

⚠ **规格 §4⑤ 的架构要求是「没有一条代码分支认识题目名」** —— 9d/9e 都实测守住了（`grep` 等值比较与 `PROBLEMS.<名>` 零命中）。**你也要守住，并在报告里贴那条 grep。**

⚠ **两条自己判的**：
- **立柱与「填色 + 写数字」是二选一还是叠加？** 这道题每格都有值，格格填色会糊成一片。**你判，并说明理由。**
- **图例怎么办？** 这道题的 `kind` 是**连续数值域**（0..6）。⚠ 9c 已经裁定过同类问题：**`kinds` 是「图例的行与顺序」，数值域用一行代表整个域**（`pathCount` 的 `'num'` 就是这么干的，**它从不被算法发出来**）。照那条办。

- [ ] **Step 3: (B) 写声明**

照 `knightPath` 的形状（**同一个范式、同一块盘，最近的可比对象**）。⚠ 自己去读它，别照我的记忆写。

- 盘固定 8×8（⚠ 自己确认 `KP_W` 那种常量的写法，别新造一套）
- 滑杆：**源的个数 1–4**，源摆四角（a1 → h8 → a8 → h1）⚠ **顺序自己定并写进注释**
- `entry: 'expand'`
- `zLabel`：z 是 **BFS 层数**（⚠ 别写死层数，9d 第 5 处就栽在这上面）
- `readout`：**最远的格子要走几步**（普通数字，`fmtInt` 就够；⚠ 别拿 9d 的 `fmtBB`）
- ⚠ **`spanZ` 要不要声明？** BFS 层数最多 7 层（0..6），比 `queens` 的 10 层还矮 —— **自己判并说明**（9e 实测：不声明会安静回落到 `SPAN_Z = 0.55`）

- [ ] **Step 4: 跑门** —— 期望 `ALGOS 往返校验` **11 → 12**、`双语 algos 普查` **11 → 12**

- [ ] **Step 5: 数一遍「动了几处」** —— `git diff --stat` 应当只有那份 HTML。
**⚠ 除立柱渲染那一处之外，若还得改 tab / 滑杆 / 相机里的任何一处 —— 停下来报告。**

- [ ] **Step 6: Commit**（显式路径）

---

## Task 3：双语文案与 `tips`

- [ ] **Step 1: 写文案常量**（⚠ `onMark().label` 显示宽度 ≤ 72、`trackLabel()` ≤ 26，CJK 算 2。**自己量，别目测。**）

- [ ] **Step 2: `tips` —— 这道题的顿悟**

**顿悟只有一个：迁移的代价是一行。**

必须写到的三样：
1. **⚠ 与 `knightPath` 的差别是队列初始塞几个起点** —— 规格「四道题的裁定」原话：*`tips` 必须明说这件事，否则它只是又一道 BFS*。**要说到她能自己切到那个 tab 对着看。**
2. **⚠ 指向一个具体的开关**（全仓规矩）：**把「源的个数」从 1 拖到 4，看最远距离掉下来** —— 实测 **6 → 5 → 3**，整张场塌一半。⚠ **这三个数自己复核一遍再写。**
3. **柱子在说什么**：高度 = 离最近那个起点几步；**源格自己是 0，贴地一格**（⚠ **0 是真值，不是「没有值」** —— 这是 9c 那条裁定的落点）。

- [ ] **Step 3: 跑门 + Commit**

---

## Task 4：浏览器验收（**不改任何代码**）

- [ ] 逐条勾，每条截图或读数存证：

1. **1 个源**：柱子立起来，最高的那根对应距离 **6**，读数写 6。
2. **⚠ 拖到 4 个源**：最远距离掉到 **3**，**整张场肉眼可见地矮了一半** —— 这是这道题的招牌画面。
3. **⚠ 源格是贴地的一根零长柱 + 一个 `"0"` 标签**（不是暗灰小点）—— 「0 是真值」那条裁定的落点。
4. **⚠ `k` 与工具① 的对照**（规格 §4⑤ 9f ③ 留给你定的那一条）：把工具① 的 `field` tab 与本题**单源**并排看一眼。**若明显别扭，报出来并给出你的建议值。**
5. **按 `3` 切侧视** —— z 是 BFS 层数，波纹一圈一层。
6. **切语言** —— 中英各一次，图例、`tips`、读数通顺无溢出。
7. **既有八道题没被误伤** —— **九个 tab 逐个 Run**，无红色状态牌。
   ⚠ **特别看 `pathCount`**：它与本题共用「每格一个标量」那条通道，**立柱渲染不该动到它的写数字**。

- [ ] **报告**：七条逐条「过 / 不过 / **未验 + 原因**」。**不许写「应该没问题」。**

---

## Task 5：工具⑤ 定版 1.7.0 + 注册表

- [ ] **三处版本号**（⚠ 是三处，自己数）
- [ ] **`chess-tools.json`**：`version` / `changelog` / `desc` / `tag`。⚠ **`tag` 在两份 FALLBACK 镜像里也有**而门**只抓 `id`** —— 三处一起改，**求值后比对**（别比原文文本）。⚠ **`desc` 不在镜像里。**
- [ ] **根注册表** —— ⚠ **不许沿用结论，自己再核一次。**
- [ ] **跑全量门 + 角标实测 `v1.7.0`**
- [ ] **Commit**（显式路径，含两份镜像）

**changelog 要写到的**：这道题**与 `knightPath` 只差一行**、**教的是迁移**；**立柱渲染是付账不是这道题的开销**；**`undefined` 分支本题走不到**（继承来的防御性代码，别写成「支持不可达格」）。

---

## 收尾

在**干净树**上：

```bash
git -C $W status --short          # 先确认是空的
node chess/core/algos/field-bfs.test.js
python3 chess/scripts/check.py
python3 scripts/sync_registry.py --check
```

期望：`check.py` exit 0，其中 **`双语 algos 普查：12 份双语`**、**`ALGOS 往返校验：… 12 份算法源码`**。

开 PR 后 `gh pr checks <PR#>`。**本地绿 ≠ CI 绿。不要自己合并，用户在对话里确认。**

---

## ⚠ 这是棋类套件的最后一节

9f 交付后，规格 §8 的十五个阶段全部完成。**收尾时要做一件额外的事**：把 §8 那张表的状态过一遍，确认没有哪一行还写着「待做」而其实已经做完（或反过来）。**这一条本身就是本阶段最容易漏的，因为它不在任何一道门里。**
