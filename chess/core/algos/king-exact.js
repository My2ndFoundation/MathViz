/* 带障碍盘上「最少的王支配所有空格」的**精确**算法源码生成器（规格 §2.1）。
   零依赖；node 与浏览器双用。编辑源，运行时被内联进 tools/*.html。

   跟 `queens.js` / `knight-path.js` / `tour-*.js` / `rook-cover.js` 是同一个形状：
   这个模块本身不算任何东西 —— 它吐出一段**字符串**。那段字符串同时是喂给
   `Interp.run` 去真跑的程序、和喂给 `Editor.mount` 摆在使用者眼前的那份源码。
   一份字符串两个用途，所以「看到的」和「跑的」不可能漂移。

   **生成出来的那段源码里的注释和命名，是写给一个没学过棋的 16 岁读者的**。
   本文件自己的注释（就是你正在读的这些）是给维护者的，两套读者，两种口吻。

   ---- 这一份和 `king-greedy.js` 是一对 ----

   共用的那一整段（建墙表、列空格、建覆盖表、`seen`/`left`、`put()`）
   **逐字节相同**，两条横线之间一个字都不许差。完整的约定、以及「阶段 5 那道
   逐行子序列的门为什么在这一对上不适用」，写在 `king-greedy.js` 的文件头里，
   那一份是这一对的主文档。**改动共用文本必须同步改那一份。**

   这一份自己多出来的东西，就是横线以下那些：`take()`（把王收回去）、
   `limit`、以及外面那圈迭代加深的 while。

   ---- 算法：迭代加深 + 「谁来管这一格」分支 ----

   一句话：先问「1 个王够不够」，不够再问 2 个、3 个…… 第一个答得上「够」的
   数就是最少的，因为比它小的都已经被否掉了。**从 0 问起**（整块盘都是墙时
   答案就是 0 —— 少了这一档会答 1，`king.test.js` 有断言钉着）。

   每一轮里怎么搜：挑一个**还没人管**的空格，它总得有人管；而能管它的王
   只可能站在它自己的九宫里（覆盖表对称，见共用段那条注释）—— 候选最多九个，
   一个一个真的摆上去试，不成就收回来换下一个。这不是启发式，是穷尽：
   「这一格总得有人管」这句话本身就把分支因子从「全盘每一格」压到了九。

   **为什么不是「枚举所有 C(n, k) 种摆法」**：那样 6×6 上 k=4 就是 5 万多种，
   而且每一种都要从头验一遍覆盖。按「谁来管这一格」分支，同样是精确解，
   实测便宜两个数量级 —— 但仍然指数，见下面那张表的第四档。

   ---- 这道题的落点：**为什么现实里要用贪心** ----

   前面五个工具都在讲「算法怎么跑」。这一题的第四档讲的是另一件事：

     7×7 `[16,24,32]` 上贪心一眨眼给出 8，**精确这一份跑不完**。
     于是我们**并不知道** 8 是不是最优。

   读数必须说「跑不完，判不了」，**绝不许说「8 是最优」**（宿主侧另算过，
   那块盘的真最优确实是 8 —— 但工具里那一屏没有算完，它不该假装知道）。
   `null` 表示「不知道」，永远不许变成数字。阶段 6 为这类情形建了整套
   `unknown` 通道（`exercise.js` 的 judge：没找到分歧又有一侧截断 → `unknown`，
   而且 `unknown` 不是 `fail`），Task 5 照它的既有呈现，别新造一套。

   8×8 更绝望：宿主侧量到 **2,700 万次**操作（简报给的是另一种计数口径下的
   7,256 万）—— **8×8 的精确支配集根本不可能进这个工具**，滑杆上限别往那儿开。

   ---- 实测（`Interp.STEP_LIMIT` = 200,000）----

   四档的两边数字放在一起才读得懂，所以整张表只在这一份里写一次。
   拿两份生成的源码各跑 `Interp.run(src, { host: {} })`，读 `result` 与
   `trace.length`；mark 次数由 `king.test.js` 的 `probe()` 量。

     档                          贪心 k / 步数      精确 k / 步数      差距   这一档演什么
     ① 5×5 [6,12,18]              4 /  3,983        4 /  18,174        0    两边一样：贪心运气好
     ② 6×6 [6,24,35]              6 /  8,154        4 /  71,742        2    **差距**出现了
     ③ 6×6 [1,4,24]               8 /  9,625        4 /  41,627        4    同一块盘、同样三堵墙，
                                                                            只挪了位置：差距翻倍
     ④ 7×7 [16,24,32]             8 / 13,578      ✗ 撞墙 200,000       ?    **跑不完，判不了**

   四档的教学轴是**差距**：0 → 2 → 4 → 不知道。三件要知道的事：

     ① **贪心四档全部跑得完**，最贵的一档（7×7）13,578 步，占上限的 6.8%。
        「一眨眼」不是修辞：第④档它至少比精确便宜 14 倍 —— 而 200,000 只是
        上限，精确那一边的真值我们并不知道有多大。
     ② **精确的步数不随盘大小平滑长**：18,174 → 71,742 → 41,627 → 撞墙。
        ③ 比 ② 大一倍的差距，却便宜四成 —— 贵不贵取决于最少王数是几、
        以及要否掉多少个更小的王数，跟「盘上有几堵墙」没有直接关系。
     ③ **第③档是换过的**（见下面那一整段）。前三档余量都在 40% 以上，
        `king.test.js` 有一条余量断言把它们钉在 120,000 之下：将来任何把
        某一档推过线的改动会**大声失败**，而不是变成一次神秘的截断。

   墙上那一档（④）**必须**撞墙，`king.test.js` 拿 `trace.truncated === true` 和
   `typeof result === 'undefined'` 两条钉着 —— 那一档要是哪天跑完了，那才是故障。

   ---- 第三档换过一次（实测逼出来的）----

   简报给的第三档是 `6×6 [6,10,25,28,32,34,35]`（贪心 7 / 精确 5），
   **它的精确解在解释器里跑不完**。简报自己预警过这一条：设计时量的是
   24,779 次算法操作，而解释器步数是算法操作数的三十倍上下（另外三档实测
   33× / 30× / 30×），乘出来 ~30 万。实测正是 200,000 撞墙、`result` 变成
   `undefined`。留着它，四档里就有两档撞墙，「差距那一课」只剩一档在演。

   换成 `6×6 [1, 4, 24]`：**同样是 6×6、同样是三堵墙，只是位置不同** ——
   贪心从 6 变成 8（整整一倍的王），精确两块都是 4。差距从 2 拉到 4，
   步数反而降到 41,627。新档的 k 由一份**穷举**（按 k 从小到大枚举所有子集，
   零剪枝）独立验过：3 个王盖不满，4 个可以（[7,10,25,28]，与第②档同解）。

   顺带一条给页面的提醒：**② 与 ③ 的贪心结果对扫描序极其敏感** ——
   把「并列取编号小的」换成倒序扫描，② 从 6 变 5、③ 从 8 变 5。
   扫描序是算法的一部分，不是实现细节（`king-greedy.js` 文件头有整条记录）。

   顺带一条实测，它订正了规格原写的「差距可见」：**矩形空盘上两份完全相同**
   （4×4 / 5×5 / 5×6 / 6×6 / 6×7 / 7×7 / 7×8 / 8×8 八种全同，8×8 都是 9）。
   空盘上王的结构太规整，贪心自然挑中 3×3 块的中心，那恰好最优。
   **差距只在有墙的时候才出现** —— 所以四档盘里没有一块空盘。 */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.AlgoKingExact = factory();
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  /* 普通字符串拼，不用模板字面量：这段文本本身会被原样贴进 html，
     反引号在那个上下文里比在这里更容易出事（照抄 queens.js 的理由）。 */

  /* ==== 以下 HEAD 与 king-greedy.js 的 HEAD **行数相同**，
     只有第 1 行的标题和第 9–10 行讲「这一份 / 另一份」的两行不同。 ==== */
  const HEAD = [
    '/* ============ 王的支配集 · 精确解 ============',
    '   王走一步能到旁边八个格子里的任何一个。所以一个王站在某一格上，连同它',
    '   自己站的这一格，一共「照顾」到九格 —— 围着它的一个九宫。',
    '   （靠边、靠角的时候九宫会缺一块，出界的那几格不算。）',
    '',
    '   盘上有些格子是墙：墙上站不了王，也不用照顾墙 —— 要照顾的是所有空格。',
    '   题目是：**最少要几个王，才能让每一个空格都被照顾到？**',
    '',
    '   这一份：**把所有摆法都试一遍**，一个不漏，所以它说得出「最少」（精确解）。',
    '   另一份：每一步只挑当下最划算的那一格，挑完就定，绝不反悔（那叫贪心）。',
    '',
    '   格子编号：sq = 行 * W + 列。第 0 行是棋盘最下面那一行，第 0 列是最左边',
    '   那一列 —— 于是 0 号格就是国际象棋里的 a1。',
    '   要反推回去：列 = sq % W，行 = (sq - 列) / W。 */',
    '',
    '/* 棋盘的宽和高，以及墙在哪儿 —— 这道题的全部旋钮。',
    '   把 BLOCKED 清空试试：**空盘上两份给出的数一模一样**（4×4 到 8×8 八种',
    '   尺寸实测全同）。空盘上王的位置太规整，贪心随手就挑中最优的那几格。',
    '   差距只在有墙的时候才出现 —— 墙一多，贪心就开始吃亏。 */',
  ];

  /* ==== 以下 COMMON 与 king-greedy.js 的 COMMON **逐字节相同** ====
     king.test.js 有一道门比这一段（两条横线之间，含横线本身）。
     改这里必须同步改那一份。 */
  const COMMON = [
    '',
    '/* ===== 从这里到下面那条分水岭为止，两份源码逐字相同 ===== */',
    '',
    '/* 哪些格子是墙。wall[sq] = 1 就是墙 —— 王站不上去，也不用照顾它。 */',
    'const wall = [];',
    'for (let i = 0; i < W * H; i = i + 1) { wall.push(0); }',
    'for (let i = 0; i < BLOCKED.length; i = i + 1) { wall[BLOCKED[i]] = 1; }',
    '',
    '/* 盘上所有的空格。要照顾到的就是这些格子，一个都不能漏。 */',
    'const empty = [];',
    'for (let s = 0; s < W * H; s = s + 1) {',
    '  if (wall[s] === 0) { empty.push(s); }',
    '}',
    'log("这块盘上有 " + empty.length + " 个空格要照顾");',
    '',
    '/* 覆盖表：covers[s] 就是「一个王站在 s 上，能照顾到哪些空格」。',
    '   王往八个方向各走一格，再加上它自己站的这一格 —— 一共九格，围成一个九宫。',
    '   出界的不算，墙也不算（墙不用照顾）。所以边上、角上的九宫会缺一块。',
    '',
    '   **王的九宫不管墙挡不挡**：挨着就是挨着，中间隔着什么都一样。',
    '   （车不是这样的：车沿直线看出去，撞上墙就停。两道题别搞混。）',
    '',
    '   这张表是**对称的**：s 照顾得到 t，t 就一定照顾得到 s —— 因为「挨着」',
    '   是相互的。这条待会儿有大用：**能照顾到某一格的王，就站在那一格的九宫里**，',
    '   所以想管住某一格，候选最多只有九个。 */',
    'const covers = [];',
    'for (let s = 0; s < W * H; s = s + 1) { covers.push([]); }',
    'for (let i = 0; i < empty.length; i = i + 1) {',
    '  const s = empty[i];',
    '  const x = s % W;',
    '  const y = (s - x) / W;',
    '  for (let dy = -1; dy <= 1; dy = dy + 1) {',
    '    for (let dx = -1; dx <= 1; dx = dx + 1) {',
    '      const nx = x + dx;',
    '      const ny = y + dy;',
    '      if (nx >= 0 && nx < W && ny >= 0 && ny < H) {',
    '        const t = ny * W + nx;',
    '        if (wall[t] === 0) { covers[s].push(t); }',
    '      }',
    '    }',
    '  }',
    '}',
    '',
    '/* 两个记录：',
    '     seen[sq]  现在有几个王照顾着这一格',
    '     left      还有几个空格一个王都没照顾到',
    '   记「几个」而不是「有没有」，是因为王有可能被收回去（精确那一份会收）——',
    '   收回去之后这一格还剩几个王照顾着，只有计数说得清。 */',
    'const seen = [];',
    'for (let i = 0; i < W * H; i = i + 1) { seen.push(0); }',
    'let left = empty.length;',
    '',
    '/* 在 s 上摆一个王：它九宫里每一格的计数加一，本来没人管的那几格就有人管了。 */',
    'function put(s) {',
    '  const c = covers[s];',
    '  for (let i = 0; i < c.length; i = i + 1) {',
    '    if (seen[c[i]] === 0) { left = left - 1; }',
    '    seen[c[i]] = seen[c[i]] + 1;',
    '  }',
    '  mark(s, "ok");',
    '  place(s, "wK");',
    '}',
    '',
    '/* ===== 分水岭：从这里往下两份不一样了 —— 差的就是「挑哪个王」 ===== */',
  ];

  const TAIL = [
    '',
    '/* 把 s 上的王收回去：九宫里每一格的计数减一，减到 0 就是又没人管了。',
    '   这是 put 的镜像，一步不多一步不少 —— 收得不干净，后面每一步都是假的。',
    '   mark 与 clear 是分开的两步：先看到这一格被判「撤销」，下一步王才消失。 */',
    'function take(s) {',
    '  const c = covers[s];',
    '  for (let i = 0; i < c.length; i = i + 1) {',
    '    seen[c[i]] = seen[c[i]] - 1;',
    '    if (seen[c[i]] === 0) { left = left + 1; }',
    '  }',
    '  mark(s, "back");',
    '  clear(s);',
    '}',
    '',
    '/* 这一轮最多允许摆几个王。下面那圈 while 会一轮一轮把它加上去。 */',
    'let limit = -1;',
    '',
    '/* 已经摆了 n 个王了，接着往下摆。在 limit 个王之内盖得满就返回 true。',
    '',
    '   这一份的主意分两步：',
    '',
    '     第一步 —— **挑一个还没人管的空格**（叫它 target）。它总得有人管吧？',
    '     第二步 —— 那么管它的那个王站在哪儿？只可能在 target 自己的九宫里',
    '                （覆盖表是对称的，上面那条注释就是为这里写的）。',
    '                于是候选最多九个，一个一个**真的摆上去**试。',
    '',
    '   试不成就把王收回来（这就是 "back"），换下一个候选。九个都不成，说明',
    '   这条路走不通，交还给上一层去换它那一步。',
    '',
    '   注意这一份**不淘汰任何候选**：它没有 "cut"，因为它不挑 —— 每一个候选',
    '   都真的摆上去走到底过。慢就慢在这儿，「最少」两个字也正是从这儿来的。 */',
    'function cover(n) {',
    '  if (left === 0) {',
    '    return true;           // 每个空格都有人照顾了',
    '  }',
    '  if (n === limit) {',
    '    return false;          // 王用光了，还有格子没人管',
    '  }',
    '  let target = -1;',
    '  for (let i = 0; i < empty.length; i = i + 1) {',
    '    if (seen[empty[i]] === 0) { target = empty[i]; break; }',
    '  }',
    '  const c = covers[target];',
    '  for (let i = 0; i < c.length; i = i + 1) {',
    '    const s = c[i];',
    '    mark(s, "try");        // 让这一位来管 target 试试',
    '    put(s);',
    '    if (cover(n + 1)) {',
    '      return true;         // 下游走通了 —— 一路把 true 传回去',
    '    }',
    '    take(s);               // 不成：把它收回去，换下一个候选',
    '  }',
    '  return false;            // 九个候选都试过了，这条路不通',
    '}',
    '',
    '/* 迭代加深：先问「0 个王够不够」，不够就问 1 个、2 个、3 个……',
    '   第一个答得上「够」的数，就是**最少**的那个 —— 因为比它小的都已经被',
    '   一个不漏地否掉了。这才是「最少」两个字唯一站得住的证法。',
    '',
    '   （为什么从 0 问起：整块盘都是墙的时候，一个王都不用摆，答案就是 0。',
    '   从 1 问起会在那一档答 1。）',
    '',
    '   看着笨，也确实笨：盘一大，问到第几个王就跑不完了。跑不完的时候',
    '   **它什么都没说** —— 不是「答案是贪心那个数」，是「不知道」。',
    '   旁边那一份从不会跑不完，这就是现实里为什么用贪心。 */',
    'let done = false;',
    'while (!done) {',
    '  limit = limit + 1;',
    '  log("试试 " + limit + " 个王够不够");',
    '  if (cover(0)) { done = true; }',
    '}',
    'log("最少要 " + limit + " 个王，一个都不能少");',
    'return limit;',
  ];

  /* source({ W, H, blocked }) → string

     三个参数都不给默认值，缺了直接抛（阶段 5 约束 6：公开导出的省略参数已经
     是本仓库抓到过八次的缺陷类）。一个默默变成 8 的 W 会让界面写着 6、跑的却是
     8，正是这个工具最不能出的错；而一个默默变成 `[]` 的 blocked 更糟 ——
     **空盘是这道题的假题**（两份给出的数一模一样，四档盘因此没有一块空盘），
     悄悄退化成假题的演示教不了任何东西。

     校验只管「是不是一块合法的盘、墙是不是都落在盘上」，**不管盘上还剩不剩
     空格、也不管跑不跑得完**：整块盘都是墙要照常吐出源码（答案 0），
     7×7 那一档明知会撞墙也要照常吐出来 —— 撞墙那一屏正是这道题的落点。

     这个函数与 king-greedy.js 的同名函数逐字相同，两边一起改。 */
  function source(opts) {
    const o = opts || {};
    if (o.W === undefined || o.W === null) {
      throw new Error('source({ W, H, blocked }) 少了 W —— 棋盘宽度没有默认值，必须写明');
    }
    if (o.H === undefined || o.H === null) {
      throw new Error('source({ W, H, blocked }) 少了 H —— 棋盘高度没有默认值，必须写明');
    }
    if (o.blocked === undefined || o.blocked === null) {
      throw new Error('source({ W, H, blocked }) 少了 blocked —— 墙没有默认值，必须写明；' +
                      '空盘请显式传 []（那是这道题的假题：空盘上贪心与精确给出同一个数）');
    }
    const W = o.W, H = o.H, blocked = o.blocked;
    if (typeof W !== 'number' || !isFinite(W) || Math.floor(W) !== W || W < 1) {
      throw new Error('W 必须是 >= 1 的整数，收到：' + W);
    }
    if (typeof H !== 'number' || !isFinite(H) || Math.floor(H) !== H || H < 1) {
      throw new Error('H 必须是 >= 1 的整数，收到：' + H);
    }
    if (!Array.isArray(blocked)) {
      throw new Error('blocked 必须是一个数组（墙的格子编号），收到：' + typeof blocked);
    }
    for (let i = 0; i < blocked.length; i++) {
      const s = blocked[i];
      if (typeof s !== 'number' || !isFinite(s) || Math.floor(s) !== s || s < 0 || s >= W * H) {
        throw new Error('blocked 里的格子必须是 0 到 ' + (W * H - 1) + ' 之间的整数，收到：' + s);
      }
    }
    return HEAD.concat([
      'const W = ' + W + ';',
      'const H = ' + H + ';',
      'const BLOCKED = [' + blocked.join(', ') + '];',
    ]).concat(COMMON).concat(TAIL).join('\n');
  }

  return { source: source };
});
